# tavernpi 技术路线

> 机制层技术文档，与《创作规划.md》平级。只写实现细节与验证状态，**不重复契约**（契约一律引用创作规划条目）。
> 验证状态标记：✅ 已核实（附证据）｜📄 仅文档声称（未经源码核对）｜⏳ 待 M0 spike（需运行实证）。
> 源码证据位置基于本地镜像 `.slim/clonedeps/repos/earendil-works__pi/`（v0.84.2，见 §5），行号随版本漂移，引用时以 tag 为准。

## 1. 技术栈与硬性要求

| 项 | 选择 | 状态 |
|---|---|---|
| 运行时 | **Node.js ≥ 24**（pi 要求 ≥22.19.0——`packages/coding-agent/package.json` engines；node:sqlite 的 backup 实证于 24.18.1，取 ≥24 LTS 收口） | ✅ |
| 语言 | TypeScript，ESM，strict | 已定 |
| SQLite 库 | **`node:sqlite`（内置）**——backup 往返已实证（§4 第 6 项），同步 API、零依赖。备选 `better-sqlite3`（若遇 node:sqlite 缺陷再切） | ✅ 实证 |
| 结构化校验 | zod（data 变更集、卡包 YAML 校验）；**subagent 结构化输出层用 typebox**——pi 的结构化输出机制是 typebox `TSchema` + constrained sampling（见 §3.2），zod schema 需经 zod→JSON Schema 映射接入 | ✅ |
| pi 依赖 | `@earendil-works/pi-coding-agent`（+ pi-ai / pi-agent-core / pi-tui），npm 依赖、不 fork（创作规划 §8.1），锁定 0.84.2 起步 | ✅ |
| YAML | **`yaml`**（活跃维护、无原生依赖、保留注释/行号报错友好；js-yaml 维护停滞） | ✅ M5 定案 |

## 2. 仓库结构

单仓 monorepo（npm workspaces）：

```
tavernpi/
├── packages/
│   ├── app/        # TUI 入口：命令解析、界面装配、模式强制接线
│   │   ├── src/        # m1-cli（M1 起；CLI 即内核 API 参照实现）
│   │   ├── acceptance/ # 里程碑故事驱动验收脚本（§6 测试策略）
│   │   └── spike/      # M0/M1 实证工件（一次性，非产品代码）
│   ├── core/       # tavernpi-core：turn pipeline 编排 + DB 层 + 快照 +
│   │               # subagent 定义/调度 + 提示词分层 + 卡包加载 + 对外 API（§10.2）
│   └── tools/      # 卡包 schema 校验/试跑 CLI（M5；V2 转换器已从规划移除，后议）
├── 创作规划.md      # 契约与决策（事实来源）
└── 技术路线.md      # 本文档
```

约束：`app` 不实现任何引擎逻辑，只消费 `core` 的 API——与「TUI 是内核 API 参照实现」（创作规划 §10.2）一致；studio 未来同样只依赖 `core`。

## 3. 复用 / 核实 / 自建总表

### 3.1 复用（npm 依赖，不修改）

| pi 能力 | 用途 | 验证状态 |
|---|---|---|
| session 树（JSONL，`id`/`parentId`，header v3 + entries） | 故事时间线（§3） | ✅ `core/session-manager.ts:41-135`；entry 类型并集 :107-135；树 = leaf 指针 + parentId 链，`getTree():1370`、`getBranch():1262`、`buildSessionContext():430`（compaction 感知） |
| `/tree` `/fork` `/clone` 机制 | 故事浏览/分叉/复制 | ✅ fork/clone 均走 `createBranchedSession` **复制**新 .jsonl（非引用，源文件不动，新 header.parentSession 指旧文件）：`session-manager.ts:1427-1545`；fork=从某 user 消息前分叉重问，clone=整条当前分支复制（`interactive-mode.ts:4936,4974`）。新 session 文件 = 新故事，与 §3.1 的 DB 目录按 session-id 隔离天然契合 |
| compaction 框架 | 章节摘要（定制 prompt） | ✅ 两个定制入口：`session_before_compact` 事件返回自定义 `CompactionResult`（**完全替换**）；`customInstructions`（仅附加）。默认 prompt 在 `core/compaction/compaction.ts`、`compaction/utils.ts:156` |
| provider/model 管理 | 各角色模型配置（§6.6） | ✅ `ModelRuntime`（`core/model-runtime.ts:130`）；多 session 共享同一 ModelRuntime 实例即共享凭证/模型 |
| extension API | core 的载体形态 | ✅ `registerTool/registerCommand/registerProvider/on(event)` + 自定义 UI 走 `registerMessageRenderer`/`sendMessage`（`core/extensions/types.ts`）；事件含 `before_agent_start`、`session_before_tree`、`session_tree`、`session_before_compact` |
| pi-tui | TUI 基础组件 | ✅ 组件清单 `tui/src/index.ts:14-57`（Text/Markdown/ScrollView/SelectList/Editor/overlay 等）；**行粒度差分渲染**（`tui-main-screen.ts:335-348,434-465`），支持已渲染区块就地替换——stylize 乐观呈现可行（详见 §6 备注） |
| package 机制（`pi` manifest） | 卡包分发（§4） | ✅ 已源码核实（M5）：manifest schema = `pi:{extensions,skills,prompts,themes}`（`core/pi-manifest.ts:3-8`）；extension = TS entry default export 工厂，jiti 转译、拿完整 ExtensionAPI（`core/extensions/loader.ts:436-516`）；**SDK 编程加载入口 = `resourceLoaderOptions.additionalExtensionPaths` / `extensionFactories`**（`core/resource-loader.ts:158-193`、`sdk.ts:75-78`），无需自动发现。决策：纯内容包自研加载器，仅代码包委托该入口（创作规划 §8「卡包加载形态」） |

### 3.2 核实结果（pi 源码 v0.84.2，已核对）

| 集成点 | 结论 | 证据与注意事项 |
|---|---|---|
| `systemPromptOverride` | ✅ 可整体替换默认叙述 | `DefaultResourceLoaderOptions` 字段（`core/resource-loader.ts`）；`buildSystemPrompt` customPrompt 分支整体替换默认叙述（`core/system-prompt.ts`）。**坑**：仍追加 append/contextFiles/skills/cwd 段——要纯净系统提示须同时清空这些；cwd 行不可剔除。另：`before_agent_start` 事件返回的 systemPrompt 是每轮最高优先级，可作运行时注入通道（`core/agent-session.ts`） |
| `agentsFilesOverride` | ✅ 可完全剔除 AGENTS.md 类上下文 | 同上文件 |
| `skillsOverride` / `promptsOverride` | ✅ 可完全替换/清空 | 同上；**坑**：`/skill:` 前缀的文件路径展开行为（`agent-session.ts`）需在自研 UI 中处理或禁用 |
| 自定义 `ResourceLoader` | ✅ 传入后内置资源发现完全失效 | 接口在 `core/resource-loader.ts`；**坑**：`AgentSession.restartRuntime` 会无条件调用其 `reload()`——我们的 loader 必须实现幂等 reload |
| `tools` / `customTools` 白名单 | ✅ 可只留自定义工具 | `CreateAgentSessionOptions`：`tools`（严格 allowlist，过滤 builtin+extension+custom **所有来源**）、`excludeTools`、`noTools:'all'|'builtin'`（`core/sdk.ts:57-103`；过滤逻辑 `agent-session.ts` `_refreshToolRegistry`）。主叙事零工具、subagent 只读 DB 工具均可表达 |
| `session.navigateTree(targetId)` | ✅ 满足回溯挂钩，语义已钉死 | 实现 `agent-session.ts:2905-3069`：① isStreaming 时抛错——UI 须先 `abort()`（官方 /tree 即如此，`interactive-mode.ts:5063-5070`）；② 目标是 user/custom_message 时 newLeaf=其 parentId，否则 newLeaf=目标本身；③ 跳转后重算 `agent.state.messages`（:3039-3041），后续生成基于目标分支上下文；④ 跨分支安全（公共祖先逻辑 `branch-summarization.ts:108-155`）；⑤ 目标不存在抛错不改状态；⑥ 原地改 leaf，不新建文件；⑦ **有 `session_before_tree` / `session_tree` 扩展钩子**——快照恢复的挂载点比「UI 调用处」更干净，挂这里；⑧ 它不触我们的 DB（预期内，恢复由我们做） |
| `SessionManager.inMemory()` | ✅ 完全不落盘 | `session-manager.ts:1567`；persist=false，`_persist()/_rewriteFile()` 直接 return（:1077,:1088）。**坑（M0 spike #4 实测修正）**：inMemory 已保证零新文件；默认 ResourceLoader/SettingsManager/ModelRuntime 仍「读」磁盘（AGENTS.md / settings.json / auth.json / models.json）——subagent 要读侧与上下文隔离才须显式传自定义三件套 |
| `createAgentSessionRuntime()` | ✅ 但并行 subagent **不用它** | `agent-session-runtime.ts:414`：单会话替换式（newSession/switchSession/fork 都先 teardown 旧会话），面向交互式 /new /resume /fork。**并行 subagent 的正确姿势**：N 路 `createAgentSession()` + 各自 `SessionManager.inMemory()` + 共享单个 `ModelRuntime`。**警告**：`session.reload()` 会清进程级全局 `apiProviderRegistry`（`pi-ai/compat.ts:207-211`），并行场景禁调 reload |
| 结构化输出机制 | ✅ tool-call 机制，非 zod | typebox `TSchema` 参数 + constrained sampling（json_schema strict，映射各 provider strict/validated 模式）：`ai/src/api/constrained-sampling.ts`、`ai/src/types.ts`、`core/model-runtime.ts:636-645`。我们的 zod schema 经 JSON Schema 映射接入。**M2 实证修正（2026-08-20）**：SDK 层**无** `toolChoice`/`structuredOutput` 入口（`CreateAgentSessionOptions` 不含；toolChoice 只在各 provider 全量 options 上，`SimpleStreamOptions` 不透传）；可用方案 = 单工具严格白名单 + `ToolDefinition.constrainedSampling:{type:"json_schema",strict:"require"}` + 提示词强制，从工具调用 arguments 取结构化数据。真实 provider（deepseek-v4-flash）实证：12 次真实 data 调用全部成功调用输出工具，zod→JSON Schema（zod v4 `toJSONSchema`）兼容良好（§7 M2） |

另核实（影响设计的情报）：

- **官方 subagent 示例是 spawn 独立 pi 进程**（`coding-agent/examples/extensions/subagent/index.ts`）——我们的 in-memory 方案比官方示例更轻，SDK 层面成立（见上）。
- **agent 核心包另有独立 JSONL v4 格式**（`packages/agent/src/harness/session/jsonl/`）——与 coding-agent 会话文件（v3）不是一回事，**别混用**；我们只面对 coding-agent 层格式。
- **subagent 系统提示注入**：除 systemPromptOverride 外，`before_agent_start` 事件可每轮覆盖 systemPrompt——NPC 预演等每轮不同系统提示的场景优先用这个通道。

### 3.3 自建（core 内部模块）

| 模块 | 职责 | 对应规划 | 状态 |
|---|---|---|---|
| turn pipeline 编排器 | §2 六步流水线的调度、并行、打回循环、失败路径 | §2、§6 | 🟡 M4 形态已交付（场景分析 → npc 并行 → 主叙事 → 轻检/打回 → stylize → data → 全统筹；模式开关细化 M6，见 §7 M4） |
| DB 层 | story.db 访问、migration、turn_seq 纪律强制、卡包 schema/seed 执行 | §5 | ✅ M1 已交付（`packages/core/src/db/`，见 §7） |
| 快照管理器 | node:sqlite backup 拍快照、祖先链查找（沿 parentId，用 `getBranch`）、原子恢复、fork/clone 初始 DB；挂载 `session_tree` 钩子触发恢复 | §3.1 ★ | ✅ M1 已交付（`packages/core/src/snapshot/`，见 §7） |
| subagent 运行时 | N 路 `createAgentSession` + inMemory + 共享 ModelRuntime；注入组装（`before_agent_start` 通道）；typebox 输出 schema；token 预算；禁 reload | §6 | ✅ M2 已交付（`packages/core/src/subagent/`，见 §7 M2）；token 预算 M3 定案（schema `.max()` 机械强制 + 事件流 usage 观测，见 §7 M3） |
| 提示词分层加载器 | 四层覆盖解析、占位符注入、回退；以自定义 ResourceLoader 为挂载点（须实现幂等 reload） | §6.5 | ✅ M2 已交付（`packages/core/src/prompts/`，见 §7 M2） |
| 卡包加载器 | 目录树读取、story.yaml/条目 zod strict 校验、引用完整性、命名空间前缀静态扫描、条目 seed（card_ref 幂等键）、mtime 缓存 + 校验失败回退上次成功快照（注入热 seed 冷） | §4.1 | 🟡 M5 实施中 |
| 设定集匹配引擎 | 子串匹配三通道（钉 > 常驻 > 触发）、token 预算裁减、`{{collection_injection}}` 渲染 | §4.1 | 🟡 M5 实施中 |
| 模式预设与查询层 | 三模式声明式预设、输入渠道校验、DB 视图过滤 | §10.1 ★ | ⬜ M6 |
| 轮中交互通道 | InteractionBroker：kind 开放命名空间 + typebox 响应校验 + 可辨识错误；UI 注册 handler | §6.7 | ✅ M1 已交付（`packages/core/src/interaction/`，见 §7） |
| 卡包校验工具 | `packages/tools`：YAML/引用/前缀校验 + 临时库试跑 schema.sql+seed.sql，报错到人 | §5.2 | 🟡 M5 实施中 |
| TUI 消息块组件 | 持文本→行缓存、暴露 setContent 的可寻址消息块（Text/Markdown.setText + requestRender 模式）——stylize 乐观呈现的宿主 | §6.6、§8.2 | ⬜ M6 |
| 对外 API 面 | §10.2 承诺面的 npm 库导出 | §10.2 | 🟡 M2–M3 逐步形成（`createStoryRuntime` + pipeline 事件流 JSONL + `onSystemPromptRender` 注入观测钩子，见 §7 M2/M3） |

## 4. M0 spike 清单（可执行验证项）

**前置条件**：pi 的 `auth.json` 已配置（`ModelRuntime.create()` 默认读取，位置见 pi 文档），且强（主叙事）/弱（subagent 与迭代调试）两档模型各至少一个可用 key。未配好前不开始 #1。

| # | 项 | 状态 |
|---|---|---|
| 1 | 环境基线：app 空壳 + pi SDK 依赖，最小 `createAgentSession` 对话 | ✅ 已实证（`spike/01-hello.ts`，deepseek-v4-flash 往返，inMemory 零落盘） |
| 2 | 资源接管：override 四件套或自定义 ResourceLoader → 实际发出的 system prompt 零编程导向残留（含 cwd 行、append 段确认） | ✅ 已实证（`spike/02`；四件套 + `appendSystemPromptOverride` + noExtensions/noSkills/noPromptTemplates/noContextFiles 后，唯一残留 = 不可剔除的 cwd 信息行（system-prompt.ts:69，无指令性）；捕获通道 `before_agent_start.systemPrompt` 与最终发送值逐字一致） |
| 3 | 工具白名单：禁全部内置工具，只留一个自定义叙事工具 | ✅ 已实证（`spike/03`；**坑：`noTools:"all"` 会连 customTools 一起过滤**（sdk.ts:249），白名单必须用 `tools` 显式 allowlist） |
| 4 | in-memory subagent：两路并行 session 互不污染；确认零盘污染需显式传的三个选项 | ✅ 已实证（`spike/04`；并行隔离成立。**修正**：零「新文件」只需 `SessionManager.inMemory()`；显式传自定义 resourceLoader/settingsManager/modelRuntime 的价值在读侧/上下文隔离，非写侧） |
| 5 | 树导航：带分支 session 调 `navigateTree`（先 abort），确认后续生成基于目标节点；验证 `session_tree` 钩子触发快照恢复的接线 | ✅ 已实证（`spike/05`；钩子顺序 `session_before_tree`→`session_tree`，payload 含 oldLeafId/newLeafId/commonAncestorId；user 目标 → newLeaf=parentId+editorText；分支最简路径 = navigateTree 回旧 user 消息再 prompt，同文件自然分叉） |
| 6 | SQLite backup 往返：写库 → backup → 改库 → 恢复 → 断言一致 | ✅ 已实证（`spike/06-sqlite-backup.ts` 自断言工件，M0 收口补录；node:sqlite 模块级 `backup(db, path)`；恢复=关闭连接→临时文件+rename 原子替换→重连） |
| 7 | compaction 定制：`session_before_compact` 返回自定义 CompactionResult 替换默认摘要 | ✅ 已实证（`spike/07`；可编程触发 = `session.compact()`（agent-session.ts:1790）+ `SettingsManager.inMemory({compaction:{keepRecentTokens:1}})` 强制 cut point；自定义摘要完全替换且默认摘要 LLM 调用被跳过） |

完成标准：1–5、7 运行实证通过（6 已过）。任何一项失败 → 回创作规划 §8.1 重新评估 fork 必要性。

### 4.1 M0 demo 定义（spike 验收单）

**形态**：`npm run spike:m0` 启动的终端 CLI 循环，单 agent、无 pipeline。

**启动**：

- 自定义 ResourceLoader（或 override 四件套）加载 `packages/core/prompts/narrator.md` 为系统提示；打印实际发出的完整 system prompt 供人工核对「编程导向零残留」（spike #2）；
- 禁全部内置工具，仅注册两个自定义叙事工具（spike #3）；
- `node:sqlite` 打开临时 story.db，建最小表：`events(id, turn_seq, summary, detail)`、`meta(k, v)`。

**循环**：用户输入一段行动 → 主叙事生成正文 → 模型自行调用工具读/写 DB → 打印正文与本轮 DB 变更。

**工具**：

- `write_event(summary, detail)`：追加 events 行（turn_seq 由工具实现自增，模型不接触）；
- `read_state()`：返回 events 全表 + 当前轮数。

**验收**：

- 连续 5 轮交互，每轮正文与 DB 内容一致（模型用 `read_state` 回应「现在发生过什么」时不编造）；
- spike #2/#3 的人工核对通过；
- #4/#5/#7 以独立小脚本实证（不混入叙事循环）。

**退出条件**：以上全过，且 session 文件（持久化主叙事）与 inMemory（#4）两种形态都跑过。

## 5. pi-mono 源码镜像

本地只读镜像（gitignored），用于 §3.2 核实：

- 路径：`.slim/clonedeps/repos/earendil-works__pi/`
- 上游：`https://github.com/earendil-works/pi.git`
- ref：`v0.84.2`（commit `914cf1472e715297caa30db4b9535d534a9eb718`，与 npm `@earendil-works/pi-coding-agent@0.84.2` 对应）
- 登记：`.slim/clonedeps.json`、`AGENTS.md`

## 6. 实现约束与规范

- **turn_seq 纪律**：DB 层在写入 API 上强制，业务代码无途径写不带 turn_seq 的行（`clock` 例外见创作规划 §5.1）。
- **快照原子性**：backup 先写临时文件，完成后原子 rename；恢复 = 关闭连接 → 原子替换 story.db → 重连。崩溃任一点可恢复。
- **并行纪律**：subagent 各自 inMemory session、共享一个 ModelRuntime；**任何会话禁调 `reload()`**（清全局 provider 注册表，殃及并行会话）；inMemory 已保证零「写」污染（M0 #4 实测），显式传自定义 resourceLoader/settingsManager/model 的目的是**读侧与上下文隔离**（默认会读 AGENTS.md / settings.json / auth.json 等）。
- **错误处理**：data 失败路径严格按创作规划 §6.1（重试上限 → 不拍快照 → 下轮补齐 → 连续 3 轮提示用户）；subagent 输出 schema 校验失败 = 该 subagent 重试，上限同。
- **测试策略**：里程碑验收 = 故事驱动集成脚本（创作规划 §7）；单测只覆盖确定性纯逻辑（快照祖先链查找、设定集匹配、schema 注释提取、zod→typebox 映射）。
- **日志**：pipeline 事件流（subagent 输入/输出/耗时/成本）从 M2 起结构化记录——它是 §10.2 承诺面，不是事后补的调试设施。
- **TUI 备注（stylize 乐观呈现已源码验证可行）**：pi-tui 行粒度差分渲染，把本轮叙事放在有状态组件里，stylize 完成后 `setText(打磨版)` + `requestRender()` 即只重写该区块行。坑：① 每行末尾自动追加 SGR/OSC8 重置，打磨版逐行自带完整样式；② 内容变短触发全屏重绘，用 `setClearOnShrink(false)` 关；③ 替换块在视口上方退化为全量重绘；④ `render(width)` 超宽抛错，需 truncate/wrap。

## 7. 里程碑交付状态

### M1 故事 DB ✅（2026-08-19 验收通过）

**交付**：
- `packages/core/src/db/`：§5.1 全量 schema + 命名 migration 框架（`schema_migrations`）+ turn_seq 运行时强制（`clock` 单例例外）+ db 工具集（`createDbTools`：get_clock / query_events / get_npc / write_event / advance_clock，turn_seq 由注入 provider 提供，模型不接触）。
- `packages/core/src/snapshot/`：独立 snapshots.db（dump = `backup()` 物理字节，未 gzip——创作规划 §3.1 已修订为「完整物理字节，当前未压缩」，压缩/增量归 §8.2 开放问题，恢复语义不变）+ 祖先链最近快照 + 原子恢复（关连接→清 WAL/SHM→tmp+rename→重连）+ `forkStoryDb` + `session_before_tree`/`session_tree` 钩子。
- `packages/app/src/m1-cli.ts`：人工验收入口（`/tree`、`/fork`、`/status`，`npm run m1:cli`，`--resume` 续写）。
- `packages/app/acceptance/m1.ts` + `packages/app/spike/09-m1-story-db.ts`：故事驱动集成验收（`npm run m1:accept` / `npm run spike:m1`）。

**关键语义钉死（验收依据）**：
- **快照绑定 = 本轮 leaf（最终 assistant）entry**，与 turn_log 同一 id。由 pi navigateTree 语义推出：导航到 user 消息 u_N → newLeaf = 其 parentId（= a_{N-1}，第 N-1 轮末），重做第 N 轮时世界必须处于第 N-1 轮末，否则事件双重落库。导航到首个 user 消息（u1）→ 祖先链无快照 → 空库兜底 = 故事初始态（「重做开头」）；hooks 的外部损伤 guard 判据 = `turn_log 非空 且 snapshots.db 全空`，不误伤该兜底。
- fork 截断点：user 目标 → parentId（目标输入前分叉重问）；assistant 目标 → 自身（clone 语义）。新故事 snapshots.db 仅含截断点一份快照。

**验收证据**（真实 LLM + 真实 pi session 树）：core 单测 32/32；`m1:accept` 30/30（回溯一致 1a/1b、回溯后再前进、fork 独立）；`spike:m1` 26/26（含 ghost entry / isStreaming 导航 / compaction 压缩区导航边界）；typecheck 0 错误。覆盖创作规划 §3.0 契约的 M1 部分（§7 M1 验收项）。

### 空间基元（migration v2）✅（2026-08-19，M1 后补丁）

创作规划「空间基元」决策的最小落地：`locations` 注册表（parent_id 表包含关系，非拓扑）+ `location_log`（镜像 time_log，subject='player'|'npc:<id>'）+ `npcs.current_location` + `events.location_id` + world_state 约定键 `player_location`（`PLAYER_LOCATION_KEY` 常量）。

- **migration**：`CORE_MIGRATIONS = [v1_core_schema, v2_spatial_primitives]`；v1 SQL 冻结，v2 = 新表 + ALTER 补列（node:sqlite 实证 ALTER ADD COLUMN 带 REFERENCES 合法且 FK 生效，旧行 NULL）；`PRAGMA table_info` 列存在性守卫幂等；旧 v1 库 open 原地升级、数据不动。
- **登记校验在 writer 层**（报错含「未登记」），与 FK 双保险：`moveSubject`（玩家→world_state、npc→npcs.current_location，单事务连同 location_log）与 `insertEvent(locationId?)`。
- **工具**：`get_location`（玩家位置父链 + 地点树）与 `move_to`（仅玩家；npc 移动 M2 起归 data subagent）；m1-cli 白名单仍为原 5 工具，工具序 5→7 不影响既有验收。
- **验收**：单测 39/39（+7）；`m1:accept` 30/30、`spike:m1` 26/26 真实 LLM 回归无破坏。

### 轮中交互通道（§6.7）✅（2026-08-19，M1 最小可用形态）

创作规划 §6.7 的 M1 落地：`packages/core/src/interaction/`。

- **broker**：`InteractionBroker.request({ kind, prompt, payload?, responseSchema, timeoutMs? })`——响应过 typebox 校验（typebox 1.x 用 `typebox/value` 子模块 `Check`/`Errors`，主包无 `Value` 命名空间）；`registerHandler` 单 handler、重复注册=替换。
- **可辨识错误**（供卡包工具 catch 降级）：`InteractionUnavailableError`（无 handler，§6.7 降级路径触发点）/ `InteractionValidationError`（响应不过 schema）/ `InteractionTimeoutError`。
- **m1-cli**：readline handler 支持内置 confirm/choice/text 三 kind（`node:readline` 'line' 事件队列——`readline/promises` 的 `question()` 在 stdin EOF 即抛 `ERR_USE_AFTER_CLOSE`，管道喂输入必踩）；`combat_check` 演示工具（choice+confirm 两次交互 → `judgeCombat` 纯规则 → 叙事），仅交互模式经 `M1RuntimeOptions.interaction` 注入——验收白名单保持 5 工具，30/30 零影响。
- **验收**：单测 49/49（+10：broker 往返/校验/超时/替换语义 + combat 判定矩阵）；`m1:accept` 30/30 回归；combat_check 管道冒烟（交互→判定→叙事→落库→快照）全链路通过。
- 结构化留痕（独立于叙事文本）与 §10.2 承诺面纳入随 M2 事件流 / M6 定型推进（§6.7 机制节）。

### M2 data subagent ✅（2026-08-20 验收通过）

**交付**：
- `packages/core/src/prompts/`：§6.5 提示词分层加载器——四层覆盖（内置 `packages/core/prompts/` < 全局 `~/.tavernpi/prompts/` < 卡包 `<packDir>/prompts/` < 故事级 `<storyDir>/prompts/`），高层读取失败/空文件回退低层 + warning；占位符语法定案 `{{标识符}}`（未知占位符原样保留 + 去重上报）。
- `packages/core/src/subagent/`：subagent 运行时——每轮空白 inMemory session + 单输出工具严格白名单 + `constrainedSampling:{type:"json_schema",strict:"require"}`（SDK 无 toolChoice 入口，§3.2 已修正）；usage 取 `getSessionStats()`，墙钟耗时，finally dispose。
- `packages/core/src/pipeline/`：
  - `changeset.ts`：变更集 zod schema（zod v4，`z.toJSONSchema()` 接入输出工具）→ 语义校验（地点/NPC 登记、时间不倒流、保留键、phase 匹配，收集全部问题）→ `applyChangeset`（校验先行 + `writer.transaction()` 原子应用；DbWriter 增 SAVEPOINT 嵌套支持，moveSubject 复用）。
  - `data-stage.ts`：`runDataStage`——data subagent 抽取落库（唯一写者，§6.1）；重试上限默认 3，校验问题反馈进下轮 userPrompt；`executor` 注入点 = 验收故障注入通道。
  - `runtime.ts`：`createStoryRuntime` 编排器（§10.2 对外 API 面 M2 形态）——主叙事**零工具**（`tools:[]` 严格白名单空数组，构建后断言 + `noTools:"builtin"` 回退）；`before_agent_start` 每轮现算 `{{db_summary}}` 整串替换系统提示（§6.0 上下文全注入）；turnSeq = turn_log 最大 +1。
  - `events.ts` + `db-summary.ts`：pipeline 事件流 JSONL（`<storyDir>/pipeline-events.jsonl`，§10.2 承诺面 M2 起）+ 确定性 DB 摘要渲染（§5.2 读取渲染由 harness 负责）。
- `packages/core/src/settings.ts`：`~/.tavernpi/settings.json` 最小形态（§6.6）——`models.narrator/data/...` 独立 `{provider,id}`，fail-open 解析，经共享 `ModelRuntime.create()` 的 `getModel` 解析。
- migration v3 `data_status(turn_seq, status, attempts, error)`：data 失败/补齐状态的持久化（status: ok/failed/compensated）；v1/v2 SQL 冻结，旧库原地升级。
- `packages/core/prompts/data.md`：data 内置提示词（抽取纪律 + 每轮必给 time_advance 自估跨度——story 未上线的降级形态，§10.1）；`narrator.md` 追加 `{{db_summary}}` 小节。
- `packages/app/src/m2-cli.ts`（`npm run m2:cli`）+ `packages/app/acceptance/m2.ts`（`npm run m2:accept`）。

**关键语义钉死（验收依据）**：
- **快照触发点迁移**：M1 每轮末必拍 → M2 起 data 落库成功后拍（§3.1 M2 形态）；data 失败轮 recordTurnLog 照常、**不拍快照**、recordDataStatus(failed)；下一轮 data 输入追加失败轮叙事文本（pendingTurns），成功后轮次标 compensated 并恢复拍快照；连续失败 ≥3 轮 onWarning 明确提示用户（§6.1 失败路径全链）。
- **快照 guard 修订**：外部损伤判据 = turn_log 非空 ∧ snapshots 全空 ∧ data_status 存在 ok 轮；全 failed（合法态）放行空库兜底。
- **主叙事零工具**：m2-cli 启动断言 `工具白名单: []`；DB 摘要经 before_agent_start 每轮注入（A 区注入观测断言：主叙事正确引用 clock 日期与玩家地点）。

**验收证据**（真实 LLM deepseek-v4-flash + 真实 pi session 树）：core 单测 93/93（+20）；`m2:accept` **37/37**（A 基线 10+1 轮：data 成功率 10/10 全部 attempts=1、time_log/snapshots == 成功轮数、锚点 NPC/地点落库、事件流齐；B 失败路径 4 场景 16 断言：重试反馈自纠/耗尽不拍快照/下轮补齐含 pending 文本/连续失败提示；C 回溯一致性 8 断言：恢复第 N-1 轮末/续写无 PK 冲突/首 user 空库兜底不误判损伤）；`m1:accept` 30/30 回归零破坏；typecheck 0 错误；m2-cli 管道冒烟通过。覆盖创作规划 §6.1 契约全部条款（含「10 轮后 DB 正确反映剧情」）。

**遗留观测（不阻塞，M3/M4 跟踪）**：
- data.md 倾向同日等值时间推进（`to_time == current_time` + span_note 时段说明，校验允许相等）：10 轮仅跨日一次。属提示词调优范畴，M4 story 场景分析上线后由「story 估跨度、data 提交」链路接替（§5.3 流转链路）。
- npc token 预算（§6.2 输出 ≤ 固定预算）随 M3 定案；subagent 运行时暂未强制输出长度上限。
- settings.json 无 models 配置时 narrator/data 均走 pi 默认模型（验收即此形态）；强弱分档配置可用但未在验收中区分。

### M3 npc subagent ✅（2026-08-20 验收通过）

**交付**：
- `packages/core/src/pipeline/npc-stage.ts`：§6.2 两模式纯模块层——在场预演 `runOnstageRehearsals`（每 NPC 一路 subagent，Promise.all 并行，单 NPC 失败丢弃降级不阻塞 pipeline）+ 离线批量 `runOffscreenBatch`（单 session 批量，重试耗尽返回 [] + warning）；`computeScenePlan` 确定性场景规划；`renderRehearsals`（主叙事隐藏批注）/ `renderOffscreenDeltasForData`（data 输入转写指示）。
- `packages/core/prompts/npc_onstage.md` / `npc_offscreen.md`：§6.5 分层角色提示词（OOC 自检必经 / 离线权威边界禁令）。
- runtime 接线（`createStoryRuntime`）：`npc: { enabled, offscreenAfterTurns, maxAttempts, executor }` 选项（**缺省关闭，M2 形态零改动**）；pipeline 顺序 = 场景规划 → npc 两阶段并行（Promise.all）→ 主叙事（`{{npc_rehearsals}}` 经 before_agent_start 注入）→ data（`offscreenDeltas` 输入）→ 快照；`TurnResult.npc` 产物（在场/触发名单 + 预演与 delta 全文）；`onSystemPromptRender` 注入观测钩子（§10.2 可观测性）。
- `packages/app/src/m3-cli.ts`（`npm run m3:cli`）+ `packages/app/acceptance/m3.ts`（`npm run m3:accept`）。

**关键语义钉死（验收依据）**：
- **M3 过渡形态（story 场景分析 M4 接管前）**：在场判定 = `npcs.current_location` == 玩家位置（确定性，§5.0 地点概念从 DB 来）；离线触发器 = K 轮未推演（`offscreenAfterTurns` 默认 5；时间跨度触发需历法换算，M4 定案）；「玩家进入其区域」由在场判定自然覆盖。
- **权威边界结构强制**：离线 delta schema 无玩家字段、`status` 枚举仅 `alive|absent`（场外致死 = 背着玩家剧变，zod 层直接拒收）；语义级违禁（合法形状但文本描述玩家损失）剩提示词层 + M4 story 轻检防线（已知边界）。
- **token 预算定案**：zod `.max()` 条数/字数上限机械强制（行动要点 ≤5×80 字、台词线索 ≤3×100 字、deltas ≤12 等）+ 事件流 usage 观测（实测预演 output 1612–3426 tokens/次，含 thinking）。
- **sys_ 内核保留键**：world_state 保留键校验扩展为 `player_location` + `sys_` 前缀；离线推演簿记 `sys_npc_offscreen_last_turn:<id>` 由编排器在 data.ok 后直写（内核簿记，同 clock 例外精神），随快照恢复（D 区实证回退）。
- **零消耗契约**：不在场且不触发推演的 NPC 零调用（onstage/offscreen 空名单跳过 subagent 创建，eventLog 无记录为证）。

**验收证据**（真实 LLM deepseek-v4-flash）：core 单测 111/111（+18）；`m3:accept` **42/42**（A 在场 3 轮×2 NPC：预演含 ooc_check 结构、系统提示机械注入验证（导演批注+预演片段）、并行证据 sum/墙钟比 1.88、预算观测；B 触发器：K-1 不触发零消耗、K 到点触发 + delta 落库（贝罗记忆 1→2）+ sys 键簿记、K 后不重复触发；C 权威边界：status:dead 恶意 delta 注入被 schema 拒绝、零影响；D 回溯：sys 键/档案/clock 随快照恢复、续写无 PK 冲突；E m3-cli 冒烟）；`m1:accept` 30/30、`m2:accept` 37/37 回归零破坏；typecheck 0 错误。覆盖创作规划 §6.2 契约全部条款（在场预演注入/多 NPC 并行/离线批量与触发器/权威边界/token 预算）。

**遗留观测（不阻塞，M4 跟踪）**：
- 验收期间曾遇 deepseek 402 余额耗尽：provider 错误在 pi 侧表现为**空 assistant 消息**（stopReason=error，无文本无 toolCall），subagent 层报「未调用输出工具」——排障时先查 provider 错误再疑 schema/提示词（诊断矩阵实证三者无关）。
- 预演 output token 含 thinking 时可达 ~3.4k/次，N 个在场 NPC 并行成本 = N ×（预演 + 主叙事变长）；M4 记录端到端延迟与成本实测（§6.6）。
- 离线触发仅轮数制；时间跨度触发器与「即将进入」预判随 M4 story 场景分析定案。

### M4 story subagent + stylize ✅（2026-08-20 验收通过）

**交付**：
- `packages/core/src/pipeline/story-stage.ts`（§6.3 两级运行）：
  - `runSceneAnalysis`：场景卡（每轮最前，全部 subagent 的结构化调度依据）——在场/离线名单、场景地点、当前故事时间、跨度估计与 to_time 建议（§5.3 流转链路正式化：story 估跨度 → data 提交）、场景目标与基调、major_event 标记；校验失败重试，耗尽降级 M3 确定性兜底（fallback 标记 + warning，不阻塞 pipeline）；active directives 纳入输入（§10.1 数据通路）。
  - `runRuleChecks`：轻检规则层（每轮必跑、零 LLM）——六条确定性断言：在场 NPC 生死/位置、在场∩离线名单互斥、场景卡时间幻觉零容忍（硬冲突）；叙事含 dead NPC 名、叙事日期早于 clock（报疑）。
  - `runReview`：轻检 LLM 层（仅报疑时触发）——已知信息越界/NPC OOC/时间线/世界事实四类语义矛盾，severity hard 并入打回依据。
  - `runOversee`：全统筹（每 K 轮默认 10 或 major_event 触发，data+快照之后不阻塞落库）——节奏/伏笔/建议/phase_advice，批注经 `{{oversee_note}}` 注入下一轮主叙事。
- `packages/core/src/pipeline/stylize-stage.ts`（§6.4，默认关闭）：`runStylize` + `stylizeFactCheck` 零漂移规则层抽查（数字多重集合 / DB 实体名出现向量 / 篇幅比 ≤1.8）；不过则反馈重试，耗尽回退原文 + drift 记录（可回退，turn_log.raw_text 留痕）。
- runtime 接线：场景卡驱动 npc 调度（M3 computeScenePlan 降为离线 K 轮兜底与 story 关闭时的全场判定）；**打回重写循环**——规则层硬冲突直接打回 / 报疑走 review，`navigateTree(u_N)` 分叉重做（契 §3.0 重骰语义，旧稿留树），打回意见经 `{{revision_request}}` 注入重写稿；上限 1 次（maxRevisions，可配）；超限放行 = turn_log.warnings 留痕 + data strictDrop。
- **strictDrop**（§6.3「冲突项不落库、只记录」）：changeset 校验升级为 per-item 问题粒度（`{item, message}`，`filterConflictingItems` 按路径剔除）；放行轮 data 剔除硬冲突项（如 time_advance 倒流）后其余照落，dropped 记录进 outcome/事件流。
- migration v4：`turn_log.warnings` 列（ALTER 幂等，`setTurnLogWarnings` 后补写）。
- `findUserEntryOnBranch`：打回重写后 u_N/u_N' 同 parentId，userEntryId 改从最终 leaf 上溯（防误中旧稿）。
- 提示词 ×4：story_scene / story_review / story_oversee / stylize（§6.5 分层角色集齐）；narrator.md 加场景卡/统筹批注/打回意见三小节；npc_onstage.md 加作者指令条（仍过 OOC 自检，§10.1 裁决权在玩家）。
- `packages/app/src/m4-cli.ts`（`npm run m4:cli`，story+npc 常开，`--stylize`/`--style` 开润色）+ `packages/app/acceptance/m4.ts`（`npm run m4:accept`）。
- **验收暴露修复（P2 bug）**：打回循环 navigateTree 前置快照存在性守卫——目标链无快照（首轮/data 失败轮）时导航会触发「空库兜底」清空 seed 与已落库事实；守卫 = 有快照走标准导航重写，无快照跳过导航直接重写（旧稿留在上下文，语义偏差可接受，绝不误清库）。

**关键语义钉死（验收依据）**：
- story/stylize **缺省关闭**（M2/M3 形态零影响：m3:accept 42/42 回归即证）；m4-cli 常开 story。
- 场景分析失败 = 确定性兜底卡 + warning；审查/统筹失败 = 放行 + warning；stylize 失败 = 回退原文——四级失败均不阻塞 pipeline。
- turn_log 记最终文本（stylize 后），raw_text 记原文（§6.4）；快照绑定最终 leaf。

**验收证据**（真实 LLM deepseek-v4-flash）：core 单测 144/144（+33）；`m4:accept` **49/49 首跑全绿**——A 场景分析 3 轮（非兜底场景卡/驱动 npc 调度一致/时间零幻觉/注入证据/data+time_log 正常）；B1 打回（dead 老王报疑→review 桩 hard→重写收敛：「在场对话」改「空座追忆」，revisions=1）；B2 超限放行+strictDrop（time_advance 倒流项剔除、正常事件照落、clock 不倒流、warnings 留痕原文记录）；C 全统筹（K=2 触发、批注跨轮注入硬证据、major_event 非 K 轮触发）；D stylize（真实路径 applied+factCheck 复核零漂移；桩 drift 路径回退原文+drift 记录+事件流 ok:false）；E m4-cli 双冒烟。回归 m1 30/30、m2 37/37、m3 42/42（首跑 36/42 系 CLI 冒烟超时+LLM 方差，重跑干净）；typecheck 0 错误。覆盖创作规划 §6.3/§6.4 契约全部条款。

**遗留观测（不阻塞，M5/M6 跟踪）**：
- 打回守卫的「无快照跳过导航」分支无单测（需真实 agent 会话），经 B 区 turn 2 快照路径验证标准重写行为。
- stylizeFactCheck 数字抽查只认阿拉伯数字（`3`→`三` 判 drift，保守方向）；短实体名可能误报——抽查策略取舍，随 M5 卡包实测校准。
- 端到端延迟实测：M4 全开一轮 = 场景分析 + N×预演 + 主叙事 +（审查）+（stylize）+ data，§6.6 成本/延迟指标随 M6 里程碑验收记录。
- C.4「data 失败不阻塞统筹」由代码结构保证（oversee 在 data 分支之外），未经注入轮次覆盖。

### M5 卡包系统 ✅（2026-08-22 验收通过）

**交付**：
- `packages/core/src/pack/`：types（zod strict 条目 schema/WorldPack/PackLoadError）+ loader（目录树加载、引用完整性含跨包、命名空间前缀静态扫描、id 冲突、KERNEL_TABLE_WHITELIST）+ cache（mtime 热更、校验失败回退上次成功快照 + warning）+ matcher（子串匹配三通道、命中数排序、预算 1500 整条裁减、refs 一级摘要行）+ seed（`<包名>_schema/_seed` 命名迁移、characters→npcs card_ref 幂等、locations parent 先种父）。
- `packages/core/src/story.ts`：`createStory`（§10.2 API 面 M5 形态）——卡包校验先行（失败不留半成品目录）→ 逐包迁移 → story.yaml 消费（历法/粒度写 clock；opening → session 根 assistant + turn_log(0) + 初始快照绑定 opening entry）→ story.meta.json（packs/defaultStyle，--resume 恢复载体）。
- runtime 接线：`renderNarratorPrompt` 抽出独立可测（单测确定性覆盖）；`{{collection_injection}}` 占位符（narrator.md 新增小节）；TurnResult.collection 留痕；stylize.styleHint 缺省读 story.meta.json defaultStyle。
- `packages/tools/`：`tavernpi-pack check`（全量校验 + :memory: 库迁移试跑两遍验幂等 + 只读冒烟，--json）/ `init`（骨架生成）/ `templates`（char-status/inventory/quest-progress 三份带中文注释 SQL 模板）+ 中文卡作者 README。
- `packages/app/src/m5-cli.ts`（`npm run m5:cli`）：`--pack` 可重复、`/packs` `/pin` `/unpin` `/reload`；--resume 从 story.meta.json 恢复 packDirs。
- `packages/app/acceptance/fixtures/`：shouling（2 character + 2 location 父子 + 1 faction + 1 plot + `shouling_favor` 表）+ minipack（多包共存测试）。

**验收证据**（真实 LLM deepseek-v4-flash）：core 单测 201/201（+57）；`m5:accept` **35/35**——A 加载校验 6 项（断链含文件名/zod strict/前缀违规/内核表写入/多包同名共存+跨包引用）、B seed 与 schema 7 项（card_ref/parent/seed 行/幂等重跑/重开不重复/story.meta.json）、C story.yaml 3 项（历法粒度/开场白首轮/文风接线）、D 注入证据 7 项真实 LLM（常驻+触发+钉三通道系统提示断言、collection.injected、refs 摘要行、正文引用注入要素、data.ok+快照）、E tools CLI 4 项、F m5-cli 冒烟 4 项；回归 m1 30/30、m2 37/37、m3 42/42、m4 49/49 零破坏；typecheck 0 错误。覆盖创作规划 §4.0 契约全部条款（设定集加载与检索式注入含引用校验与条目 seed、schema/seed 执行与校验工具、多包共存前缀校验、卡作者 SQL 文档与表模板库）。

**验收暴露修复**：fixer 中断残留——before_agent_start 钩子未接 renderNarratorPrompt（collection_injection 占位符滞留原文、TurnResult.collection 恒 undefined），D 区断言暴露后修复（钩子改调 renderNarratorPrompt + runTurn 置入注入输入 + TurnResult 挂 collection）。

**遗留观测（不阻塞，M6 跟踪）**：
- 多包提示词层：PromptLayerDirs.packDir 单目录，m5-cli 取首包 prompts/；多包提示词合并策略 M6 定。
- 包代码（extensionEntryPaths → additionalExtensionPaths）挂载点未接（主叙事零工具，挂哪个 session 待 M6 模式设计）。
- createStory 的 opening assistant 消息带 provider 占位 `tavernpi/story-opening`，runtime 启动时 pi 模型恢复告警一行（「Could not restore model」→ 回退当前模型），无害留痕。
- 卡包自定义表不纳入 data changeset（创作规划 §8.2 新增开放问题，M6 评估）。
