# tavernpi 创作规划

> Harness 形态的 SillyTavern。基于 pi coding agent（pi-mono）架构二次创作。
> 本文档是项目级事实来源：任何架构修改先更新本文档，再改代码。
> 范围说明：tavern studio 已拆分为独立项目（独立仓库、独立规划，见 `tavern-studio/创作规划.md`），本文档只约束 tavernpi 内核及其对外 API 承诺面（§10.2）。
>
> **文档结构约定**：每个能力节分「契约」与「机制」——
> - **契约**：对特定受众（玩家/卡作者/外部 UI）的可观察承诺，可验证；变更契约 = 破坏性变更，里程碑验收只引用契约。
> - **机制**：当前实现选择，可自由替换；替换只需更新本节与 §8 决策记录，不动契约。
> - 开放问题集中于 §8.2；§8 中 ★ = 承重机制（替换成本接近契约，变更需里程碑级评审）。

## 1. 项目定位

tavernpi 是一个**故事引擎 harness**：把 pi 的「编码 agent harness」范式平移到互动叙事领域。

| pi 概念 | tavernpi 对应 |
|---|---|
| session（JSONL 树） | 一个故事。分支 = if 线 / 重骰 / 回溯续写 |
| extension / pi package | 世界包：设定集 + SQL + 可选代码（§4，插件形式，npm/git 分发） |
| 内置工具 read/write/bash | 叙事工具：db 读写、NPC 查询、世界状态查询 |
| subagent（官方不带，extension/SDK 自建） | data / npc / story / stylize 四类职能 subagent |
| compaction | 章节摘要（定制压缩策略，保留叙事要素） |
| SDK `createAgentSession()` | subagent 的实现载体 |
| TUI | 故事交互界面（消息流 = 叙事文本流） |

**设计原则（理念层，变更需重写本文档）**：

- **对话历史是草稿，数据库才是事实。** 模型上下文里只放近期叙事 + DB 快照摘要；一切长期记忆（事件、阶段、NPC 特征）落库，由 subagent 维护与读取。
- **一个主叙事 session 执笔。** 多 NPC 由 npc subagent 预演、主叙事统一呈现，不做群聊模式。

产品形态：tavernpi 是**内核**（可嵌入的故事引擎，自带 TUI）。GUI 外壳 tavern studio 为**独立项目**，以库形式嵌入本内核；本规划仅约束内核对其暴露的 API 承诺面（§10.2）。关系类比 Chromium 与基于它的浏览器。

## 2. 架构总览（机制视图）

```
玩家输入（行动/对话；创造模式可附剧情大纲指令，见 §10.1）
  │
  ▼
┌─ turn pipeline（core extension 编排）────────────────────────┐
│ 1. story subagent 场景分析（每轮，轻量模型）：               │
│    在场 NPC、需推演的离线 NPC、当前故事时间与本轮时间跨度、  │
│    场景目标与基调                                            │
│ 2. npc subagent 并行（只读 DB）：                            │
│    在场 × N → 行为预演(结构化)；离线批量 × 1 → 活动推演      │
│ 3. 主叙事 session 生成正文（无 DB 工具，上下文全注入）        │
│    输入 = 系统提示(世界包设定集) + 近期叙事 + DB 摘要         │
│           + 场景分析 + NPC 预演(隐藏批注) + 玩家输入          │
│ 4. story subagent 审查（条件触发）→ 通过 / 打回(有上限)      │
│ 5. stylize subagent 文风调整（可选启用）：只改文风不动事实     │
│ 6. data subagent 落库（唯一写者）。输入 = 最终文本 +          │
│    玩家输入 + 各 subagent 结构化产物(离线 delta、时间推进)    │
└───────────────────────────────────────────────────────────────┘
  │
  ▼
叙事输出 · DB 快照(写入独立 snapshots.db，绑定 session entry id)
```

四层结构：

- **harness 层**：独立 app，npm 依赖 `@earendil-works/pi-coding-agent`（不 fork，见 §8.1），使用其 session 树、extension、SDK、compaction、provider/model 管理。
- **core extension 层（tavernpi-core）**：turn pipeline 编排器 + DB 工具集 + 快照/回溯一致性 + 四类 subagent 的定义、调度与提示词分层加载（§6.5）。
- **内容插件层**：世界包 = pi package（§4：设定集 + SQL + 可选代码）。纯内容包（无代码）零代码发布，用声明式 YAML 条目 + 约定的 extension 壳。
- **UI 层**：内核自带 TUI（基于 pi-tui：叙事阅读、NPC 面板、故事树、状态页），是无 GUI 环境与开发调试的主力界面。GUI 外壳（tavern studio）为独立项目（§10.2）。不接 Web 前端，不做群聊模式（已决策）。

## 3. Session = 故事

### 3.0 契约（给玩家）

- 一个故事的全部时间线（含所有分支）可浏览、可从任意节点续写、可分叉为独立新故事、可复制当前线。
- 回溯到任意节点后：DB、时钟、NPC 记忆与该节点的叙事状态一致；回溯之后可以再次前进到更新的分支。
- fork / clone 出的新故事与原故事互不影响。
- 章节摘要压缩历史后，回溯仍然可用（落到压缩点状态）。

### 3.1 机制：pi session 继承 + DB 快照一致性（★ 承重）

直接继承 pi session 机制：

- 一个 session 文件 = 一个故事的全部时间线（含所有分支）。
- `/tree` = 故事树浏览、任意节点续写；`/fork` = 从某节点开新故事文件；`/clone` = 复制当前线。
- `/compact` 定制为「章节摘要」：压缩时保留未解决的伏笔、在场 NPC 状态、当前阶段目标（通过 extension 自定义 compaction prompt）。
- 故事元数据（标题、使用的世界包及版本、DB 文件路径）存于 session 文件头部 entry 或同名 sidecar。

DB 与分支一致性（session 树可回溯，SQLite 不可自动回溯）：

- **快照存储**：每轮 data 写完 DB 后（M1 过渡形态：CLI 循环每轮末拍，M2 起由 data 落库后触发），core extension 用 SQLite backup API 生成 story.db 的完整副本，写入**独立的 `snapshots.db`**（与 story.db 同目录），表结构 `snapshots(turn_seq, session_entry_id, dump)`。dump = 当时 story.db 的完整物理字节（backup API，含 turn_log/time_log/directives 等全部表；当前未压缩，压缩/增量存储见 §8.2，不改变恢复语义）。snapshots 存于独立文件，不存在 dump 自包含导致的递归增长。每轮快照绑定**本轮 leaf（最终 assistant）entry**——导航到 user 消息 u_N 时 newLeaf = 其 parentId（= 第 N-1 轮末），重做第 N 轮时世界必须处于第 N-1 轮末，否则事件双重落库。
- **恢复**：玩家经 `/tree` 跳转到 entry E 时，取 **E 的祖先链上最近**的快照（entry id 无全局序，恢复语义按祖先关系定义），用其 dump 原子替换 story.db。snapshots.db 不动——晚于 E 的快照仍保留，回溯之后可以再次前进。**边界**：祖先链无快照（如导航到首个 user 消息，「重做开头」）→ 恢复为故事初始空库；但 `turn_log` 有历史且 snapshots.db 全空视为外部损伤，拒绝擦库并报错到人。
- **fork / clone**：以目标 entry 对应快照的 dump 作为新故事的初始 story.db；新故事的 snapshots.db 初始化为仅含该一份快照。
- **data 失败与快照**：data 落库失败的轮次**不拍快照**，未落库内容记入 turn_log；下一轮 data 的输入追加未落库轮的叙事文本，一并抽取补齐，补齐后恢复正常快照节奏（见 §6.1）。
- **compaction 交互**：章节摘要压缩 session 历史后，被吞并 entry 对应的快照仍保留；回溯目标落在压缩区内部时，恢复到压缩点 entry 的快照。
- **挂钩点**：SDK 提供 `session.navigateTree()` 原地跳转，且导航前后有 `session_before_tree` / `session_tree` 扩展钩子（已源码核实，v0.84.2）——快照恢复逻辑挂在 `session_tree` 钩子上，无需 patch harness（§8.1；语义细节见技术路线 §3.2）。
- **存储量**：O(轮数 × DB 大小)，dump 当前未压缩存储；叙事规模数据下可接受，压缩/增量见 §8.2。
- 开放问题：快照增量存储（§8.2），不改变恢复语义。

## 4. 插件：世界包（设定集）

### 4.0 契约（给卡作者）

- 一个世界包 = **一部作品的完整设定**：设定集（多类型条目）+ SQL + 可选代码——不是一名角色一张卡。主角也是设定集中 `type=character` 的一个条目，无特殊格式地位（参考 novelWriter 的项目结构：设定树按类分根，每条目一文档）。
- 纯内容包**零代码**可发布：YAML 条目 + SQL 数据，无需写 extension 代码。
- 设定集自研轻量格式，**不兼容 SillyTavern V2**；提供单向转换器：V2 卡 → 一个 character 条目，V2 内嵌世界书 → 同包对应类型条目。只保证「能轻易转」，不保证双向兼容。
- 条目保留酒馆蓝灯/绿灯语义（常驻 vs 触发），其余从简；**注入永远检索式**（触发 / 常驻 / 手动钉），设定集全文任何时候都不整体进上下文。
- 条目可交叉引用（`type:id`）；引用完整性由加载器校验，断链即报错到人。
- `schema.sql` / `seed.sql` 在故事创建时对故事 DB 执行，幂等。
- 多包共存；包间表名与 world_state 键强制 `包名_` 前缀（内核保留键除外，§5.1）；包内条目 id 唯一，冲突即加载失败并报错到人；跨包引用写作 `包名:条目id`。
- 高级包可带代码：注册自定义工具（如专属判定系统）、自定义 UI、turn pipeline 钩子、轮中交互（§6.7）。
- **静态/动态分界**：设定集 = 静态设定（世界初值与作者意图）；运行时状态（NPC 状态/位置/记忆、事件）全在 DB。设定集改动作用于故事创建/重载时机；运行中故事的设定热更新机制为开放问题（§8.2）。

### 4.1 机制

插件 = pi package（`package.json` 带 `pi` manifest），npm/git 分发。纯内容包目录约定：

```
my-world/
├── package.json        # pi manifest
├── story.yaml          # 作品级元数据：标题、默认历法/粒度（§5.3）、开场白、默认文风（§6.4）
├── collection/         # 设定集：目录树，每条目一文件，文件名即条目 id
│   ├── characters/     # type=character：主角与 NPC 同构
│   ├── locations/      # type=location：seed locations 注册表（§5.1）
│   ├── objects/        # type=object：关键物品
│   ├── factions/       # type=faction：势力/组织
│   └── plot/           # type=plot：剧情线/大纲（directives 的种子来源，§10.1）
├── prompts/            # 可选：覆盖 subagent 提示词（§6.5）
└── db/
    ├── schema.sql      # 本包的变量表/势力/物品等表与种子数据
    └── seed.sql
```

轻量格式设计目标不变：**最小 token 消耗**，字段少而结构化。条目通用结构（细节 M5 定稿）：

```yaml
# collection/characters/shen-qiu.yaml（文件名即条目 id：shen-qiu）
type: character            # 目录即类型；字段供加载器校验
name: 沈秋
keys: [沈秋, 守陵人]         # 触发词（绿灯）；空 = 仅常驻/手动钉
always_on: false           # 常驻（蓝灯）
position: system           # 插入位置（取值集 M5 定：system / recent / …）
refs: [location:royal-tomb, faction:old-crown]   # 交叉引用
# —— 以下为 character 特化字段 ——
identity: |                # 核心人设（压缩结构化）
personality:               # 性格调色盘 / 三面性
voice: |                   # 该角色口吻指导（可选；缺省用 story.yaml 默认文风）
dialogue_examples: []      # 少量对话示例
```

character 条目编写规范借鉴 tavern-cards 方法论（结构化表达压 token、性格调色盘、三面性），落盘格式自研。

加载语义：

- **检索式注入**：core 按 keys 触发 / always_on 常驻 / 手动钉匹配条目，按 `position` 注入；命中条目的 `refs` 只展开一级摘要行（名称 + 一行核心），不递归全文展开。
- 主角与 NPC 条目按位置注入主叙事（等价 SillyTavern 的角色描述/性格/场景）；npc subagent 在场预演时注入对应 character 条目全文（§6.2）。
- **文风分流**：`story.yaml` 默认文风与条目 `voice` 供 stylize（§6.4）；stylize 关闭时默认文风附加进主叙事系统提示（文风指导永不丢消费者）。
- `story.yaml` 的历法/粒度写入 `clock` 初始值（§5.3）；开场白作为故事第一轮的主叙事种子。
- **条目 seed DB**：故事创建时，characters 条目自动 seed `npcs` 表（`card_ref = 包名:条目id`），locations 条目自动 seed `locations` 注册表；`db/schema.sql` / `seed.sql` 只承载卡包自定义表（§5.2）。
- **V2 → tavernpi 转换器**：独立工具（M5）。

## 5. 数据库设计

### 5.0 契约

- （给玩家）每故事独立 DB，故事间互不影响。
- （给系统）所有写入带 `turn_seq`（`clock` 单例是显式例外）；快照绑定 `session_entry_id`。
- （给系统）所有 agent 的地点概念都从 DB 来（`locations` 注册表 + 实体当前位置），不从叙事文本里猜——与时间同一原则（§5.3）。
- （给玩家）记忆不删只降权（salience 衰减），删除 = 标记遗忘事件——NPC 的「记得」可追溯。
- （给卡作者）schema 版本化，卡包的 `schema.sql` 走 migration 表。

### 5.1 机制：schema

每故事一个目录 `~/.tavernpi/stories/<session-id>/`，内含 `story.db`（业务与审计数据）与 `snapshots.db`（快照，见 §3.1）。

```sql
-- 时间
clock(id INTEGER PRIMARY KEY CHECK (id = 1), current_time, calendar, granularity)  -- 单例当前值,历史在 time_log;不带 turn_seq,是「所有写入带 turn_seq」原则的显式例外
time_log(turn_seq, from_time, to_time, span_note)                 -- 每轮时间推进记录

-- 叙事世界
events(id, turn_seq, story_time, type, summary, detail, participants, location, location_id, created_entry_id)  -- location 自由文本留作叙事描述; location_id 引用 locations(登记校验)
locations(id, name, parent_id, detail)                          -- 地点注册表; parent_id 表达包含关系(如 王城>庭院), 不构成完整拓扑
location_log(turn_seq, subject, from_location, to_location, note)  -- 位置变更记录; subject = 'player' 或 npc:<id>, 镜像 time_log
phases(id, name, started_turn, ended_turn, goals, status)          -- 故事阶段/幕
world_state(key, value, turn_seq)                                  -- 天气、经济等键值; 约定键 player_location = 玩家当前 location_id

-- NPC
npcs(id, name, card_ref, status, current_location)                -- status: alive/dead/absent...; current_location 引用 locations
npc_traits(npc_id, trait, weight, source, turn_seq)                 -- 性格特征，可演化
npc_memories(id, npc_id, turn_seq, kind, content, salience)         -- 记忆；salience 供检索排序
npc_relations(npc_a, npc_b, disposition, turn_seq)                  -- 关系/好感

-- 一致性
turn_log(turn_seq, session_entry_id, user_input, narrative_text, raw_text)  -- raw_text: stylize 前原文(未启用则同 narrative_text)
snapshots(turn_seq, session_entry_id, dump)                         -- 存于独立 snapshots.db(§3.1)
directives(id, turn_seq, content, status)  -- 创造模式的剧情大纲指令(作者意图,非世界事实);status: active/done/revoked(§10.1)
```

### 5.2 变量系统（MVU 的底层化；★ 承重：SQL 是一等创作接口）

**契约（给卡作者）**：

- **SQL 是一等创作接口，不设「普通用户糖层」**。NPC 特征、状态、记忆、环境、事件全都以表的形式存在，卡作者定义表就是在定义世界的一部分——SQL 是内容本体，不是高级用户的可选项。
- 卡作者只需掌握 SQL 子集（`CREATE TABLE` + 种子 `INSERT`）；常用表模板（角色状态栏、好感度、物品栏、任务进度……）可直接抄改。
- 工具链提供 schema 校验与试跑：对新库执行 `schema.sql` + `seed.sql`，报错到人。
- **表/列的注释就是给 data subagent 的填写说明**（「favor: -100~100，初见通常为 0」），语义与结构在同一处维护，不另搞一套说明文件。

**契约（给玩家）**：

- 状态界面与叙事天然同步（快照绑定，见 §3.1）；无需正则替换/HTML hack。

**机制**：

- 变量 = story.db 里的表，类型与约束在 SQL/zod 层表达；结构化输出层（subagent 输出、交互响应）用 typebox——zod 负责校验语义，typebox 负责 constrained sampling 输出通道，映射关系见技术路线 §1。
- **LLM 只填表，不管格式**：写入全部走 data subagent 的结构化输出（zod schema 校验），LLM 从不接触序列化格式、不写 EJS、不维护文本状态栏。这是相对 SillyTavern MVU 的核心改进——格式负担与出错面从 LLM 移到确定性代码。
- **读取渲染由 harness 负责**：注入主叙事上下文时 core extension 把相关表渲染成紧凑文本，渲染代码确定性、不占 LLM 精力。
- **UI 直接查库**：状态界面 = DB 查询结果的渲染。
- schema 注释提取进 data subagent 提示词的机制随 M2 提示词分层机制一起实现。

### 5.3 时间系统

**契约**：

- （给系统）所有 agent 的时间概念都从 DB 来，不从叙事文本里猜。
- （给玩家）故事时间不倒流；时间推进跨度合理（睡一觉 vs 一句话有区别）。
- （给系统）时间流逝是离线 NPC 推演的触发器之一（见 §6.2）：跨度超阈值时离线世界必须跟上。

**机制**：

- **`clock` 表**存当前故事时间、历法、粒度。历法与粒度由卡包配置（回合制 tick / 小说式弹性时间 / 真实历），默认弹性时间。
- **流转链路**：story 场景分析估计本轮时间跨度 → 主叙事在时间感知下写作（注入当前故事时间）→ data subagent 提交时间推进（结构化字段，写入 `time_log` 并更新 `clock`）。校验：不倒流、跨度合理性。
- **`events.story_time`** 锚定每个事件发生时的故事时间，时间线矛盾检查（story 轻检）以此为据。

## 6. 四类 subagent

### 6.0 总契约

- （给系统）pipeline 内 data 是**唯一写者**（★ 承重），其余 subagent 只读 DB；外部写入走内核写入 API（§6.1）。
- （给系统）主叙事不持有 DB 工具，上下文全由编排器注入——省 token 且消除主叙事越权读写。
- （给玩家）每个 agent 角色的模型与 thinking level 可独立配置；npc 可按卡覆盖。

机制总则：subagent 均通过 pi SDK `createAgentSession()` 创建 in-memory session，工具白名单收紧；每轮从空白启动，上下文由编排器注入（DB 路径、相关 NPC id、近 N 轮叙事、当前阶段、当前故事时间）；输出一律走结构化 schema。

角色速览（机制视图）：

| 角色 | 时机 | DB | 输出 | 频率 |
|---|---|---|---|---|
| story 场景分析 | 每轮最前 | 读 | 场景卡 | 每轮 |
| npc 在场 | 主叙事前 | 读 | 行为预演 | 每在场 NPC 一次 |
| npc 离线 | 主叙事前 | 读 | 活动 delta | 触发器驱动，批量一次 |
| 主叙事(session 本体) | 叙事前注入齐备后 | 无（上下文全由编排器注入） | 正文 | 每轮 |
| story 审查/统筹 | 主叙事后 | 读 | 通过/打回/批注 | 条件触发 |
| stylize | 审查后、data 前 | 读 | 改写后正文 | 可选，默认关 |
| data | 每轮最后 | **写（唯一）** | 状态变更集 | 每轮 |

### 6.1 data（写者，唯一）

**契约**：

- （给系统）每轮叙事引起的世界状态变更**且仅**经 data 落库；pipeline 外不存在第二条写路径（外部编辑器走内核写入 API，见下）。
- （给玩家）data 失败不阻塞叙事呈现，且不丢数据：未落库内容会在后续轮次补齐。
- （给外部 UI）受信任写入（如编辑器直改 NPC 特征/记忆）必须走内核写入 API——同样过校验、带 turn_seq、写后立即拍快照；不允许绕过 API 直接改库。

**机制**：

- **时机**：每轮叙事定稿后（若启用 stylize，指 stylize 改写后的最终文本）。
- **输入**：本轮最终叙事文本 + 玩家输入 + 本轮各 subagent 的结构化产物（story 场景卡、npc 离线活动 delta、时间推进）。
- **职责**：对比输入与 DB 现状，产出状态变更集（新事件、阶段推进、NPC 记忆/特征/关系 delta、world_state 更新、时间推进），校验后落库。
- **约束**：变更集先过 zod/SQL 约束校验；失败重试有上限。
- **失败处理（已定案）**：重试耗尽后不阻塞叙事呈现，但该轮**不拍快照**，未落库内容记入 turn_log；下一轮 data 的输入追加未落库轮的叙事文本，一并抽取补齐，补齐后恢复正常快照节奏。连续失败超阈值（默认 3 轮）则明确提示用户。

### 6.2 npc（读者 × N，并行；在场预演 + 离线推演两种模式）

**契约**：

- （给玩家）NPC 言行与其性格特征、记忆一致（不 OOC）；NPC 不表现出上帝视角（只言行它此刻应知/应记的内容）。
- （给玩家）离线世界不背着玩家剧变：能影响玩家的世界线变动必须先经主叙事呈现——玩家不会突然面对「房子被烧毁」这类既成事实。
- （给系统）不在场且不触发推演的 NPC 不消耗调用。

**机制**：

- **时机**：主叙事生成前，story 场景分析之后。
- **在场模式（每 NPC 一个 session，并行）**：读对应 NPC 的特征、记忆、关系、当前处境，输出**行为预演**：意图、情绪、下一步行动要点、台词线索（bullet，非散文）、它不知道的事。
- **OOC 自检（预演必经环节）**：输出前对照该 NPC 的性格特征与记忆检查拟议台词/行动。预演输出含 `ooc_check` 字段（通过 / 风险点 + 修正后版本）；story 轻检同步把「NPC 言行与特征/记忆矛盾」列入硬冲突清单，双保险。
- **离线模式（批量，单个 session）**：对出现过但不在场的 NPC 推演其当前活动。批量处理——一个 subagent 按区域/阵营分组推演多个 NPC，而非每个 NPC 一次调用。输出每 NPC 的活动 delta（位置变化、在做的事、新增记忆、状态变化），**作为结构化产物交给 data subagent 落库**（单写者规则不破，见 §6.1 输入定义）。
- **离线推演触发器**（由 story 场景分析判定）：距上次推演的故事时间跨度超阈值 / 玩家即将进入其所在区域（「区域」= `locations` 注册表中的地点或其 parent 链祖先，场景分析据 `npcs.current_location` 判定）/ 连续 K 轮未推演。不是每轮全量。
- **权威边界**：离线推演只演化 NPC 自身（位置——写 `npcs.current_location` 并记 `location_log`、记忆、状态、关系），不得制造直接影响玩家的既成事实。
- **约束**：只读 DB；输出 ≤ 固定 token 预算。

### 6.3 story（场景分析 + 统筹，两级运行）

**契约**：

- （给系统）每轮所有 subagent 的调度有结构化依据（场景卡），不是各 subagent 自行判断。
- （给玩家）叙事硬冲突（时间线、生死、已知信息越界、NPC OOC）会被发现：要么打回修正，要么留痕（turn_log warning）且冲突变更不落库。
- （给系统）正文永远由主叙事 session 执笔——story 改事实、不改文风。

**机制**：

- **场景分析（每轮，轻量模型，主叙事之前）**：读玩家输入 + 近期叙事 + DB 摘要，产出结构化场景卡：在场 NPC 名单、需离线推演的 NPC 名单及原因、当前故事时间与本轮时间跨度估计、场景目标与基调。创造模式下另读 `directives` 表中 active 的大纲指令，作为**作者意图**纳入场景卡并下达给 npc/主叙事（§10.1）。
- **审查与统筹（条件触发，主叙事之后）**：
  - 轻检（每轮可选）：读本轮叙事 + DB 摘要，查硬冲突。有冲突 → 打回主叙事重写（上限 1–2 次；超限放行，warning 写入 turn_log，且该轮 data 落库时冲突项按严格校验执行——与 DB 硬冲突的变更项不落库、只记录）。
  - 全统筹（阶段切换/重大事件/每 K 轮）：评估节奏、伏笔回收、NPC 目标推进，产出给主叙事的**约束与批注**（下一阶段建议、该埋的钩子）。

### 6.4 stylize（可选启用，每轮最后）

**契约**：

- （给玩家）开启 stylize 后**事实零漂移**：事件、对白语义、时间、NPC 行为结果与改写前一致；原文留痕（turn_log.raw_text），可对比、可回退 stylize 的破坏。
- （给玩家）文风目标可配置：取自世界包文风字段（`story.yaml` 默认文风 + character 条目 `voice` 可选覆盖，§4.1），玩家可在故事级覆盖；默认关闭。

**机制**：

- **时机**：story 审查通过之后、data 落库之前。data 基于最终文本抽取，turn_log 记录最终文本。
- **职责**：按文风目标重写本轮叙事——用词、句式、节奏、视角一致性。
- **硬约束实现**：提示词中明确「零事实漂移」，story 轻检的规则层可对 stylize 前后文本做事实一致性抽查（实体/数值比对）。
- **动机**：主叙事模型与文风不必绑定——可以用推理强但文风平的模型生成，再由 stylize 统一打磨。

### 6.5 提示词配置（所有 subagent）

**契约**：

- （给卡作者/玩家）每个 subagent 角色的提示词都可在四层覆盖：内置默认 < 全局 < 卡包 < 故事级；高层覆盖低层，出错时回退到下一层默认。
- （给卡作者）卡作者调文风与填表语义，（给玩家）玩家调叙事偏好，互不干涉。

**机制**：

- 提示词为 markdown 文件，不是代码常量。分层：内置默认（tavernpi-core 自带）< 全局 `~/.tavernpi/prompts/<role>.md` < 卡包 `prompts/<role>.md` < 故事级（玩家在故事内调整）。
- 角色集合：`narrator`（主叙事系统提示的骨架部分）/ `story_scene` / `story_review` / `story_oversee` / `npc_onstage` / `npc_offscreen` / `data` / `stylize`。
- 提示词支持占位符注入（NPC 名、当前故事时间、在场名单等运行时值），占位符语法 M2 定案。

### 6.6 成本与延迟（策略与机制）

本节的契约只有一条（已列于 §6.0）：每角色模型独立可配。以下为策略与机制：

- subagent 用小快模型（策略）；`~/.tavernpi/settings.json` 里 `models.narrator/story/npc/data/stylize` 各自指定模型与 thinking level（机制）；不设「档位预设」的硬性抽象，配置先行、预设后补（决策，见 §8）。
- npc subagent 并行；离线推演合并为单次批量调用；DB 快照摘要在 turn 内复用，利用 provider prompt cache。
- 场景分析固定每轮一次但用最小模型；审查轻检可降级为规则检查（关键词/状态断言），LLM 轻检仅在规则报疑时触发。
- stylize 默认关闭，开启后每轮 +1 次调用且玩家看到文本的延迟增加（需等 stylize 完成）。
- 典型一轮调用数：1（场景分析）+ N（在场 NPC）+ 0~1（离线批量）+ 1（主叙事）+ 0~1（审查）+ 0~1（stylize）+ 1（data）。调用链以串行为主，端到端延迟是体验关键指标，M4 起在里程碑验收中记录实测值。
- 开放问题：stylize 乐观呈现（§8.2）。

### 6.7 轮中交互通道（最小接口，小游戏/判定 UI 的挂载点）

**契约**：

- （给卡作者）卡包自定义工具可在执行中发起**轮中交互**：挂起 await 玩家响应，响应经工具声明的 schema 校验后返回给工具；无 UI handler 时工具收到明确错误（可自行降级为文本提问或默认分支），不崩溃、不挂死。
- （给玩家）交互请求可见、可响应；交互响应是 user 自己的输入，三个模式均放行（不属于 §10.1 生存/冒险要拒绝的「非 user 角色行为」）。
- （给外部 UI）交互请求/响应走**单一通道**（注册 handler），TUI 与外部 UI 各自实现自己的 handler；kind 为开放命名空间，新交互类型（如动作小游戏）只需新 kind + 新 handler，不动内核。

**机制**：

- `InteractionBroker`（core）：`request({ kind, prompt, payload?, responseSchema, timeoutMs? }) → Promise<校验后响应>`；`registerHandler(fn)` 由 UI 层注册。`responseSchema` 用 typebox（与 subagent 结构化输出同一套），响应不过校验 = 工具收到校验错误（可重问或降级）。
- kind 命名空间：内置 `confirm` / `choice` / `text`；卡包自定义 `包名:kind`（如 `combat:qte`），与 §4 多卡命名空间前缀同一原则。`payload` 为 kind 特定的开放参数（选项列表、小游戏参数）。
- M1 阶段即提供最小可用形态：m1-cli 注册 readline handler（confirm/choice/text 三种内置 kind 可玩）；富渲染（TUI overlay、studio 原生小游戏界面）后续按 kind 逐步增强，接口不变。
- 交互结果经工具结果进入本轮叙事，由 data 照常落库；回溯重做该轮 = 重新发起交互（与 §3.0 redo 语义一致）。原始结果的结构化留痕（独立于叙事文本）随 M2 pipeline 事件流一起落地。
- M6 随 §10.2 API 面定型时把本通道纳入承诺面（M6 前属逐步形成中的内核 API，按 §10.2 版本纪律可调整；本节契约语义不变）。

## 7. 里程碑

验收均引用契约（行为可观察），不锁定机制。

- **M0 harness 基座**：初始化独立 app（npm 依赖 pi SDK，不 fork）；跑通「自定义系统提示 + 叙事工具 + SQLite」的单 agent 叙事循环。以最小 spike 复验 §8.1 的 SDK override 结论与 `navigateTree` 挂钩点，并验证 §3.1 快照机制（backup API 往返：写库 → 快照 → 改库 → 恢复 → 状态一致）。
- **M1 故事 DB**：schema（含 `clock`/`time_log`）+ db 工具集 + turn_log + 快照/恢复（独立 snapshots.db）。验收 §3.0 契约：手工验证 `/tree` 回溯后 DB 与时钟一致（含回溯后再前进、fork 后新故事 DB 独立两个场景）。
- **M2 data subagent**：验收 §6.1 契约——每轮自动抽取落库（含时间推进与校验）；校验失败路径（重试/不拍快照/下轮补齐/连续失败提示）。「10 轮后 DB 正确反映剧情」。附带落地**提示词分层加载机制**（§6.5，后续所有 subagent 复用）。
- **M3 npc subagent**：验收 §6.2 契约——在场预演注入主叙事；多 NPC 并行；离线批量推演与触发器；离线推演不制造影响玩家的既成事实；token 预算。
- **M4 story subagent**：验收 §6.3 契约——「人为制造时间线矛盾被打回」；超限放行的 turn_log warning 与严格校验落库；全统筹 + 阶段管理；stylize（§6.4 契约：事实零漂移抽查）。
- **M5 卡包系统**：验收 §4.0 契约——设定集加载与检索式注入（含引用校验、条目 seed DB）；db schema/seed 执行与校验工具；多包共存（命名空间前缀校验）；V2 → 轻量格式转换器；卡作者 SQL 文档与表模板库。**输入渠道校验判定机制定案（见 §8.2）**。
- **M6 模式与体验**：验收 §10.1 契约——**内核级模式预设**（三模式的 subagent 启用集合、输入渠道校验、DB 视图过滤实现；冒险模式「与 user 相关」v0 规则定案，见 §8.2）；验收 §3.0 契约收尾——章节摘要 compaction（含压缩区快照语义）、`/swipe`（基于分支重生成）；故事树视图美化、NPC 状态栏 UI；**内核对外 API 收敛**（§10.2 承诺面定型，供 TUI 与外部 UI 集成）。
- **tavern studio**：独立项目、独立规划，不在本文档里程碑内（§10.2）。

## 8. 决策记录（机制台账）

★ = 承重机制：替换成本接近契约，变更需里程碑级评审，不能作为普通重构进行。

| 问题 | 决策 |
|---|---|
| harness 形态 | SDK 应用，不 fork（§8.1；fork 是已记录的逃生通道） |
| 卡格式 | 世界包设定集：目录树多文件条目（characters/locations/objects/factions/plot），单角色卡完全吸收为 character 条目；轻量自研，V2 仅提供单向转换器（§4） |
| 变量系统 ★ | DB 表为底层本体，LLM 只填表，SQL 为一等创作接口（§5.2） |
| 写者纪律 ★ | pipeline 内 data 唯一写者；外部写入走内核写入 API（§6.1） |
| 快照机制 ★ | 快照存独立 snapshots.db；恢复取祖先链最近快照、原子替换 story.db；fork/clone 以目标快照为新故事初始 DB；data 失败轮不拍快照、下轮补齐（§3.1） |
| UI 路线 | 内核自带 TUI 为长期主力，不接 Web；GUI 外壳拆分独立项目 |
| 模型搭配 | 每个 agent 角色独立配置模型与 thinking level，不设「档位预设」硬性抽象，配置先行、预设后补（§6.6） |
| 群聊模式 | 不支持。一个主叙事 session 执笔，多 NPC 由 npc subagent 预演、主叙事统一呈现（§1 设计原则） |
| 时间系统 | 一等数据：`clock`/`time_log` 表，story 估跨度、data 提交推进并校验（§5.3） |
| 空间基元 | 最小内核基元：`locations` 注册表 + `npcs.current_location` + 玩家位置（world_state 约定键）+ `location_log` + 登记校验；完整地图拓扑/移动规则由卡包 SQL 自定义，内核不强制（§5.1） |
| 轮中交互通道 | InteractionBroker 单一通道：kind 开放命名空间（内置 confirm/choice/text，卡包 `包名:kind`）+ typebox responseSchema 校验 + 无 handler 明确错误；M1 起 readline 最小可用，M6 纳入 §10.2 定型（§6.7） |
| 离线 NPC 推演 | npc 离线批量模式，场景分析按触发器调度；只演化 NPC 自身；结果作为结构化产物经 data 落库（§6.2） |
| story 场景分析前置 | 每轮主叙事之前先做结构化场景分析，作为全部 subagent 的调度依据（§6.3） |
| stylize subagent | 支持，默认关闭；只改文风不动事实；文风目标来自卡包、玩家可覆盖（§6.4） |
| 提示词配置 | 分层可覆盖 markdown 文件：内置 < 全局 < 卡包 < 故事级（§6.5） |
| 主叙事 DB 访问 | 主叙事不持有 DB 工具，上下文全由编排器注入（§6.0） |
| 多卡命名空间 | 卡包表名/键强制 `包名_` 前缀，冲突即加载失败（§4） |
| tavern studio | 独立项目、独立仓库、独立规划；内核以 npm 库形式被同进程嵌入，只承诺 §10.2 的 API 面 |
| 内置模式 ★ | 创造/生存/冒险三模式为内核级声明式预设（非 UI 隐藏），TUI 与外部 UI 共同强制（§10.1）；M6 实现 |
| NPC OOC 防护 | npc 预演必经 OOC 自检（`ooc_check` 字段）+ story 轻检列入硬冲突，双保险（§6.2） |

### 8.1 Q1 事实核查：SDK 应用 vs fork

**结论：提示词污染不构成 fork 理由。** pi 的编程导向提示词在 SDK 层可完全替换，已核实（《docs/sdk.md》）：

- `systemPromptOverride: () => "..."` —— 系统提示整体替换；
- `agentsFilesOverride` —— AGENTS.md 上下文文件可剔除/替换；
- `skillsOverride` / `promptsOverride` —— skills、prompt 模板可完全自定义；
- `tools` / `customTools` —— 工具白名单 + 自定义工具，内置 bash/read 等可全禁；
- 传自定义 `ResourceLoader` 时，pi 的资源发现完全失效，由 tavernpi 全权接管。

**真正决定 fork 与否的是 UI 和分发形态**，而 tavernpi 的 UI（叙事阅读体验、NPC 面板、swipe、状态界面）与 pi 的编码 TUI 差异巨大，本来就要自建。因此：

- **采用 SDK 应用形态**：tavernpi = 独立 app，依赖 `@earendil-works/pi-coding-agent`（+ pi-ai / pi-agent-core / pi-tui），不 fork。
- 例外情形（届时再 fork，且只浅 fork coding-agent 包）：需要改 session 文件格式、或 §3.1 所需的树导航挂钩 SDK 不暴露。SDK 已有 `session.navigateTree(targetId)`（已核实），故事回溯在自研 UI 里由我们发起调用，快照恢复逻辑随之触发，大概率不需要 fork。
- 注意：以上结论出自文档核查，**M0 必须以最小 spike 实测复验**后再作为架构基座依赖。

### 8.2 开放问题清单

| 问题 | 定案期限 |
|---|---|
| 输入渠道校验（生存/冒险拒绝非 user 角色输入）的判定机制：LLM 分类（每轮 +1 调用、有误判）vs 启发式（不可靠）；误判时的用户申诉路径 | M5（随卡包系统一起落地） |
| 冒险模式 DB 视图过滤「与 user 相关」的 v0 确切定义（user 自身条目 + 与 user 有直接关系的 NPC 条目？视野模拟是远期演进） | M6（内核查询层 API 收敛依赖此定义） |
| 冒险模式 subagent 启用集合是否强制全开（当前推断与生存一致） | M6 前 |
| stylize 乐观呈现（先呈现原文、stylize 完成后就地替换） | 渲染能力已源码核实可行（pi-tui 行粒度差分 + setText/requestRender，技术路线 §6）；剩余决策为 UX 取舍，M6 评估 |
| 快照增量存储（替代每轮全量 dump） | 远期；不改变 §3.1 恢复语义的前提下随时可做 |
| 设定集热更新（运行中故事的条目增改如何生效：重载注入？seed 差异合并？） | M5（随卡包系统一起落地） |

## 9. 参考事实（pi 已核实能力）

- Session：JSONL 树，`id`/`parentId`，`/tree` `/fork` `/clone`，见 `docs/session-format.md`。
- Extension：TS 模块，`registerTool/registerCommand/on(event)`，可自定义 UI、compaction、provider；官方明示 sub-agents/plan mode 用 extension 自建。
- SDK：`createAgentSession({ sessionManager: SessionManager.inMemory(), modelRuntime })`；多 session 用 `createAgentSessionRuntime()`。
- Package：npm/git 分发，`pi` manifest 声明 extensions/skills/prompts/themes。
- 工具白名单：`--tools` / `--exclude-tools` / `--no-builtin-tools`；SDK 侧 `tools`/`customTools` 选项（已核实）。
- SDK 资源接管：`systemPromptOverride` / `agentsFilesOverride` / `skillsOverride` / `promptsOverride` / 自定义 `ResourceLoader`（已核实，docs/sdk.md）。
- SDK 树导航：`session.navigateTree(targetId)` 原地跳转（已核实，docs/sdk.md）。

## 10. 内置模式与对外集成

### 10.1 三个内置模式

**契约（给玩家）**：

| 能力 | 创造模式 | 生存模式 | 冒险模式 |
|---|---|---|---|
| subagent 启用 | 用户自选开关 | 强制全开 | 待确认（推断与生存一致，见 §8.2） |
| 输入渠道 | 行动/对话 + 剧情大纲指令 + 交互响应（§6.7） | 仅 user 角色的行动/对话 + 交互响应（§6.7） | 仅 user 角色的行动/对话 + 交互响应（§6.7） |
| 设定/提示词查看与编辑 | 全部 | 全部 | 仅 user 相关条目 |
| DB 查看 | 全部 | 全部 | 仅 user 相关条目（内核查询层过滤，v0 规则 M6 定案） |
| 定位 | 小说创作辅助 | 高代入 RP（世界透明） | 高代入 RP（信息迷雾） |

- **剧情大纲指令（创造模式独有）**：玩家可输入「剧情大纲」类指令控制 NPC 行为与剧情走向。指令与性格冲突时，预演标注风险，**由用户决定强行或调整**——裁决权在玩家。
- **输入渠道校验（生存/冒险）**：内核拒绝非 user 角色行为的输入（如直接命令 NPC、指定剧情结果），提示用户改写为角色行动。此处只承诺行为契约，不承诺实现方式（判定机制为开放问题，§8.2）。
- **DB 视图过滤（冒险）**：按「与 user 相关」过滤；远期可演进为「user 角色应知的」视野模拟。

**机制（★ 承重：信任边界）**：

- 模式是**内核级的声明式预设**（不是 UI 层的隐藏）：{subagent 启用集合、允许的输入渠道、DB 视图过滤器}。TUI 与外部 UI（如 studio）走同一套内核强制执行，绕过界面也无法越权。M6 实现。
- 剧情大纲指令落 `directives` 表（作者意图，非世界事实，status: active/done/revoked）；story 场景分析把 active 指令纳入场景卡下达给 npc/主叙事（§6.3）；npc 预演把指令视为剧本要求，仍须过 OOC 自检（§6.2）。
- DB 视图过滤在内核查询层实现，v0 规则 M6 定案（候选定义：user 角色自身的条目 + 与 user 有直接关系的 NPC 条目）。

### 10.2 对外集成（tavern studio 等）

tavern studio 是**独立项目、独立仓库**（类比浏览器之于 Chromium）：技术选型（Tauri/Electron/其他）、编辑器与查看器设计、发布节奏均由其自身规划（`tavern-studio/创作规划.md`）决定，本文档不展开。拆分后本文档只约束内核对其的承诺：

**契约（给外部 UI）**：

- **嵌入方式**：外部 UI 以 npm 库形式同进程嵌入 tavernpi 内核，不走 RPC。
- **API 承诺面**：故事生命周期（创建/加载/分支/回溯）、带模式过滤的 DB 查询、受信任的 DB 写入 API（§6.1 写者例外）、提示词分层管理（§6.5 各层读写与覆盖链查询）、agent/模型配置、模式管理（§10.1）、pipeline 事件流（各 subagent 的输入/输出/耗时/成本观测）、轮中交互通道（§6.7 handler 注册）。
- **版本纪律**：API 在 M2–M6 逐步形成、M6 定型；定型后的语义变更视为内核破坏性变更，走版本化。
- 内核自带 TUI 与外部 UI 共用同一套内核强制逻辑（模式、校验、过滤），互为第一方消费者——TUI 即 API 的参照实现与联调对象。
